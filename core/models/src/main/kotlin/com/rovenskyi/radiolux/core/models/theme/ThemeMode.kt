package com.rovenskyi.radiolux.core.models.theme

/**
 * Available theme modes for the app.
 * AUTO mode uses time-based switching (9:00-18:00 = Light).
 */
enum class ThemeMode {
    LIGHT,
    DARK,
    AUTO
}
